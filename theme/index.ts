import { colors } from './colors';
import { fontWeights, lineHeights, fontSizes, fontFamily } from './fonts';
import { sizes } from './sizes';
import { type Theme } from './theme.interface';
export const theme: Theme = {
    fontFamily,
    sizes,
    colors,
    fontWeights,
    lineHeights,
    fontSizes,
};
